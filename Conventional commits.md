1. **type** = `feat` (adds), `fix` (repairs), else `chore` (everything else).
2. **!** if it **breaks** existing use. (skip if not)
3. **summary** = one-line **imperative** (“add…”, “fix…”) — no period.
> Example: `fix(api)!: return 404 on missing user #123`